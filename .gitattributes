# Git for Windows checks out CRLF by default, and prettier.config.mjs sets no endOfLine, so its default
# `lf` would reject every file on the windows-latest leg before a single test ran.
* text=auto eol=lf
